# DinnerDecider 仕様書

## バージョン
- v1.0: 初期リリース（診断機能・履歴機能）
- v1.1: フィードバック機能追加（2026-03-20）
- v1.2: セッション単位の履歴フィルタリング実装（2026-03-21）
- v1.3: 外食・自炊モード分岐機能追加（2026-04-08）
- v1.3.1: 食べログURLを位置情報なしのシンプルURL方式に変更（2026-04-24）

## システム概要

「今夜なに食べる？」をテーマにした夕食提案アプリ。35問の質問バンクからランダムに5問を出題し、回答をスコア化してストレス・疲労・緊張・活気・社会性・欲求・混乱の7カテゴリ得点を算出する。scikit-learnの決定木モデルで10グループに分類し、その日の気分に合った夕食を提案する。ユーザーのフィードバックデータを蓄積して再学習することで提案精度を向上させる。

- バックエンド: Python + Flask
- DB: SQLite（dinner.db）
- フロントエンド: HTML + CSS + JavaScript
- ML: scikit-learn（決定木 DecisionTreeClassifier）
- 起動: `python app.py` → http://localhost:5000

---

## 機能一覧

### v1.0 機能

#### 診断機能
- 35問の質問バンク（`questions_data.py`）から7カテゴリ各1問、計5問をランダムに出題
- 各回答値（0〜3）をカテゴリスコアとして集計
- 決定木モデルでグループ（G1〜G10）を判定し、対応する夕食4品を提案
- G10「普通の日」のみランダムで1品ハイライト表示
- 診断結果をレーダーチャートと棒グラフで可視化
- 結果はDBに保存され、URLで再参照可能（`/result/<result_id>`）

#### 履歴機能
- 過去の診断結果一覧をページネーション（10件/ページ）で表示（`/history`）
- フィードバック件数の表示
- フィードバック50件以上で再学習ボタンを表示

### v1.1 機能

#### フィードバック機能
- 診断結果画面で「実際に何を食べたか」と「満足度（1〜5）」を収集
- 提案4品から選択するボタンUI＋「その他」自由入力
- 同一診断結果への再送信は上書き保存（INSERT or UPDATE）
- フィードバックデータをMLモデルの再学習に活用

### v1.3 機能

#### 外食・自炊モード分岐

- トップページに「🍽️ 外食を決める」「🍳 自炊を決める」の2ボタンを追加（従来の「診断スタート！」ボタンを廃止）
- モードはURLクエリパラメータ（`mode=out` / `mode=in`）で診断画面に引き継ぎ、JSのhidden inputを経由して送信
- `POST /api/submit` に `mode` フィールドを追加し、グループ判定後に `foods_out` または `foods_in` から提案料理を選択
- `quiz_results` テーブルに `mode` カラム追加（既存レコードは `'in'` でデフォルト）
- 結果画面でモードバッジ（外食モード / 自炊モード）を表示
- 外食モード時: 料理カードをタップすると食べログの料理名検索へ新しいタブで遷移（`https://tabelog.com/search/?sw=料理名`）。位置情報取得は行わない
- 自炊モード時: クックパッドのレシピ検索へ遷移（従来通り）
- 履歴画面の各カードにモードバッジを表示

#### 料理データ分類（GROUP_INFO の foods_out / foods_in）

各グループの提案料理を外食用（`foods_out`）と自炊用（`foods_in`）に分離。
モードに応じた4品が選ばれ診断結果として保存される。

| カテゴリ | 料理例 |
|---------|------|
| 外食のみ | 焼肉・寿司・ステーキ・居酒屋系・激辛ラーメン・こってりラーメン・トンカツ・うなぎ・しゃぶしゃぶ・たこ焼き・エスニック料理・イタリアン・タイ料理・インドカレー |
| 自炊のみ | 雑炊・茶碗蒸し・肉じゃが・野菜炒め・卵かけご飯・味噌汁定食・お茶漬け・親子丼・生姜焼き・豚キムチ・麻婆豆腐・ビビンバ・サラダチキン・鶏の照り焼き・焼き魚・プルコギ |
| どちらでもあり | 鍋・うどん・そば・カレー・ハンバーグ・パスタ・唐揚げ定食・牛丼・焼き鳥・和定食・刺身定食・おにぎり・おでん |

### v1.2 機能

#### セッション単位の履歴フィルタリング
- Flaskセッション（クッキーベース）にUUIDの `session_id` を保存（`app.before_request` で自動生成）
- 診断結果はその `session_id` と紐づけて `quiz_results` テーブルに保存
- 履歴ページ（`/history`）は同一 `session_id` の結果のみ表示（他ユーザーのデータは見えない）
- ブラウザを閉じるとセッションクッキーがリセットされ `session_id` も変わるため履歴も消える（ログイン不要）
- MLの再学習（`get_training_data`）は `session_id` でフィルタせず全件を対象とする

---

## データベース設計

### quiz_results テーブル

| カラム | 型 | 制約 | 説明 |
|--------|----|----|------|
| id | INTEGER | PRIMARY KEY AUTOINCREMENT | 診断結果ID |
| created_at | TEXT | NOT NULL | 診断日時（YYYY-MM-DD HH:MM:SS） |
| stress | REAL | NOT NULL | ストレスカテゴリスコア（0.0〜3.0） |
| fatigue | REAL | NOT NULL | 疲労カテゴリスコア（0.0〜3.0） |
| tension | REAL | NOT NULL | 緊張カテゴリスコア（0.0〜3.0） |
| vitality | REAL | NOT NULL | 活気カテゴリスコア（0.0〜3.0） |
| social | REAL | NOT NULL | 社会性カテゴリスコア（0.0〜3.0） |
| desire | REAL | NOT NULL | 欲求カテゴリスコア（0.0〜3.0） |
| confusion | REAL | NOT NULL | 混乱カテゴリスコア（0.0〜3.0） |
| group_id | INTEGER | NOT NULL | 分類グループID（1〜10） |
| group_name | TEXT | NOT NULL | グループ名（例: 限界戦士） |
| foods | TEXT | NOT NULL | 提案料理リスト（JSON配列文字列） |
| highlight | TEXT | | G10のときランダム選出された1品（NULL可） |
| session_id | TEXT | NOT NULL DEFAULT '' | ブラウザセッションID（UUID） |
| mode | TEXT | NOT NULL DEFAULT 'in' | 診断モード（'out'=外食 / 'in'=自炊） |

### feedback テーブル

| カラム | 型 | 制約 | 説明 |
|--------|----|----|------|
| id | INTEGER | PRIMARY KEY AUTOINCREMENT | フィードバックID |
| result_id | INTEGER | NOT NULL REFERENCES quiz_results(id) | 対応する診断結果ID（外部キー） |
| actual_food | TEXT | NOT NULL | 実際に食べた料理名 |
| satisfaction | INTEGER | NOT NULL CHECK(satisfaction BETWEEN 1 AND 5) | 満足度（1〜5の整数） |
| created_at | TEXT | NOT NULL | 送信日時（YYYY-MM-DD HH:MM:SS） |

---

## APIエンドポイント一覧

| メソッド | パス | 説明 | リクエスト | レスポンス |
|---------|------|------|----------|----------|
| GET | `/` | トップページ | - | HTML（診断件数を表示） |
| GET | `/quiz` | 診断画面 | クエリパラメータ: `mode`（`out`/`in`、デフォルト`in`） | HTML |
| GET | `/api/questions` | ランダム5問取得 | - | `{"questions": [...]}` |
| POST | `/api/submit` | 診断結果の送信・保存 | `{"answers": {q_id: {"value": int, "category": int}, ...}, "mode": "out"|"in"}` | `{"result_id": int}` |
| GET | `/sw.js` | Service Worker配信（PWA用） | - | JavaScript（Cache-Control: no-cache） |
| GET | `/result/<result_id>` | 診断結果詳細画面 | パスパラメータ: result_id | HTML（存在しない場合はトップへリダイレクト） |
| GET | `/history` | 履歴一覧画面（現在のセッションIDに紐づく結果のみ返す） | クエリパラメータ: `page`（デフォルト1） | HTML |
| POST | `/api/feedback` | フィードバック送信 | `{"result_id": int, "actual_food": str, "satisfaction": int}` | `{"success": bool, "message": str}` |
| GET | `/api/feedback/count` | フィードバック件数取得 | - | `{"count": int}` |
| POST | `/api/retrain` | MLモデル再学習 | - | `{"success": bool, "message": str}` |
| GET | `/api/stats` | 診断統計取得 | - | `{"count": int, "distribution": [...]}` |

---

## フィードバック機能 詳細仕様

### UI仕様

`templates/result.html` および `static/js/feedback.js` で実装。

- **提案4品の選択ボタン**: 診断結果の提案料理4品がそれぞれボタンとして表示される
- **「その他」ボタン**: 提案外の料理を入力するためのボタン。選択するとテキスト入力欄（`#feedbackOtherInput`）が表示される
- **星評価（1〜5）**: `feedback.js` がDOMに星ボタン（`★`）を1〜5で動的生成。ホバーでプレビュー表示、クリックで確定
- **送信ボタン**: 料理の選択（その他の場合は入力必須）と星評価の両方が揃った場合のみ活性化（`disabled` 解除）
- **送信後の完了メッセージ**: フォームが非表示になり「ありがとうございました！🙏 あなたのフィードバックでAIが賢くなります」というメッセージを表示

### バリデーション仕様

**クライアントサイド（feedback.js）**
- 料理ボタンまたは「その他」テキスト入力が空でない場合のみ送信ボタン活性化
- 星評価が1以上の場合のみ送信ボタン活性化

**サーバーサイド（app.py: `POST /api/feedback`）**
- `actual_food`: 必須。空文字・空白のみ不可（`.strip()` 後に空ならエラー）
- `satisfaction`: 必須。1〜5の整数のみ受付（`isinstance(satisfaction, int)` かつ `1 <= satisfaction <= 5`）
- `result_id`: `quiz_results` テーブルに存在すること（`db.get_result(result_id)` で確認）
- 同一 `result_id` への重複送信: 既存レコードを上書き（UPDATE）。存在しない場合は新規登録（INSERT）

### ML再学習への組み込み

`ml_model.py` の `retrain()` メソッドおよび `food_to_group()` 関数で実装。

- **food_to_group()**: 料理名から対応するグループIDを検索する関数。`GROUP_INFO` の `foods` リストを走査し、完全一致または前方一致でグループIDを返す。マッチしない場合は `None` を返す
- **満足度4以上の重みづけ**: `satisfaction >= 4` のフィードバックは同じデータを2回追加することで学習の重みを増加させる（高満足度データを優遇）
- **フリーテキスト（未登録料理）のMLスキップ**: `food_to_group()` が `None` を返した場合、そのフィードバックはML訓練データに含めない
- **合成データとの統合**: 再学習時は初期合成データ（各グループ120件）＋実ユーザーデータ＋フィードバックデータを結合して学習

### 再学習トリガー

- **条件**: `quiz_results` テーブルのデータが50件以上であること（フィードバック件数ではなく診断件数）
- **場所**: 履歴画面（`/history`）に再学習ボタンが表示される（`feedback_count` が渡され表示制御に使用）
- **エンドポイント**: `POST /api/retrain`
- **モデル保存**: 再学習後のモデルは `model.pkl` に上書き保存される
- **レスポンス例（成功時）**: `{"success": true, "message": "N 件のデータで再学習しました（精度: XX.X%）"}`
- **レスポンス例（データ不足時）**: `{"success": false, "message": "再学習にはデータが50件必要です（現在 N 件）"}`

---

## 診断グループ一覧

| グループID | グループ名 | 外食提案 | 自炊提案 | 特徴 |
|-----------|----------|---------|---------|------|
| G1 | 限界戦士 | こってりラーメン、焼肉、トンカツ、牛丼 | 唐揚げ定食、牛丼、生姜焼き、ハンバーグ | 高ストレス＋高疲労 |
| G2 | ご褒美モード | 寿司、ステーキ、うなぎ、しゃぶしゃぶ | 刺身定食、鶏の照り焼き、ハンバーグ、パスタ | 高欲求＋高活気 |
| G3 | 癒しが欲しい | おでん、鍋、うどん、そば | 茶碗蒸し、雑炊、鍋、うどん | 高緊張＋高疲労 |
| G4 | 健康意識モード | 和定食、刺身定食、そば、うどん | サラダチキン、野菜炒め、焼き魚、和定食 | 高活気、低ストレス・疲労・欲求 |
| G5 | 省エネモード | そば、うどん、牛丼、おにぎり | お茶漬け、おにぎり、雑炊、卵かけご飯 | 高疲労、低活気・欲求 |
| G6 | 発散したい | 激辛ラーメン、タイ料理、インドカレー、焼肉 | 豚キムチ、麻婆豆腐、ビビンバ、プルコギ | 高ストレス、低緊張 |
| G7 | 社交モード | 居酒屋系、たこ焼き、しゃぶしゃぶ、焼き鳥 | 鍋、焼き鳥、パスタ、ハンバーグ | 高社会性＋高活気 |
| G8 | しんみりモード | うどん、そば、おでん、和定食 | 肉じゃが、親子丼、味噌汁定食、卵かけご飯 | 高混乱、低社会性・活気 |
| G9 | チャレンジャー | エスニック料理、イタリアン、タイ料理、インドカレー | パスタ、カレー、ビビンバ、プルコギ | 高活気＋高欲求、低疲労・緊張 |
| G10 | 普通の日 | 牛丼、焼き鳥、ハンバーグ、カレー | カレー、ハンバーグ、生姜焼き、パスタ | バランス型（全カテゴリ1〜2） |

---

## 既知の課題・今後の対応

- **料理名の重複によるMLマッピングへの影響**: `food_to_group()` は先頭からの前方一致・完全一致で最初にマッチしたグループIDを返す仕様のため、複数グループに存在する料理（例: 焼肉→G1優先、鍋→G3優先）はML学習時に先着グループとして扱われる
- **フィードバック50件未満では再学習不可**: 診断件数が50件未満の場合は再学習APIがエラーを返す
